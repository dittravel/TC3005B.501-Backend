# Copy this file to backup.env and customize values for your environment.

# Environment label used in backup filenames.
BACKUP_ENVIRONMENT=development

# Base directory where local backups and logs are stored.
BACKUP_BASE_DIR=/var/backups/dittravel
BACKUP_LOG_FILE=/var/backups/dittravel/backup.log

# Keep backups newer than this number of days.
MARIADB_RETENTION_DAYS=14
MONGODB_RETENTION_DAYS=14

# Source mode:
# - local: connect directly to localhost/remote host using mysqldump/mongodump
# - docker: execute dump inside docker compose services
# - auto: try docker service first, fallback to local
MARIADB_SOURCE=auto
MONGODB_SOURCE=auto

# Directory containing docker-compose.yml for DB services.
COMPOSE_PROJECT_DIR=/home/dittravel/TC3005B.501-Backend
MARIADB_DOCKER_SERVICE=mariadb
MONGODB_DOCKER_SERVICE=mongodb

# MariaDB connection settings.
MARIADB_DB_NAME=CocoScheme
MARIADB_USER=travel_user
MARIADB_PASSWORD=supersecret
MARIADB_HOST=127.0.0.1
MARIADB_PORT=3306
MARIADB_BACKUP_SUBDIR=mariadb

# MongoDB connection settings.
MONGODB_DB_NAME=fileStorage
MONGODB_HOST=127.0.0.1
MONGODB_PORT=27017
MONGODB_USER=
MONGODB_PASSWORD=
MONGODB_AUTH_DB=admin
MONGODB_BACKUP_SUBDIR=mongodb

# Remote copy (optional). Disable with false if not needed.
REMOTE_BACKUP_ENABLED=false
REMOTE_BACKUP_HOST=
REMOTE_BACKUP_USER=
REMOTE_BACKUP_TARGET_DIR=/var/backups/dittravel
# Optional SSH private key path. Leave empty to use default ssh agent/key.
REMOTE_BACKUP_SSH_KEY=

# Automatic schedule (cron expression), configurable by superadmin.
# Set false to remove cron job automatically.
BACKUP_AUTOMATION_ENABLED=true
# Every 6 hours by default.
BACKUP_CRON_SCHEDULE="0 */6 * * *"

# Optional DB VM sync for UI saves. When enabled, the backend pushes this file to the DB VM
# and runs install-backup-cron.sh there so the UI change takes effect immediately.
BACKUP_REMOTE_SYNC_ENABLED=false
BACKUP_REMOTE_SYNC_HOST=
BACKUP_REMOTE_SYNC_USER=
BACKUP_REMOTE_SYNC_TARGET_DIR=/home/dittravel/TC3005B.501-Backend
BACKUP_REMOTE_SYNC_SSH_KEY=

# Restore settings (CLI-only disaster recovery).
# Restore source mode:
# - local: connect directly with mariadb/mongorestore
# - docker: execute restore inside docker compose services
# - auto: try docker service first, fallback to local
MARIADB_RESTORE_SOURCE=auto
MONGODB_RESTORE_SOURCE=auto

# Optional explicit backup file paths. Leave empty to use the latest backup file.
# Example MariaDB: /var/backups/dittravel/mariadb/CocoScheme_production_20260101_030000.sql.gz
# Example MongoDB: /var/backups/dittravel/mongodb/fileStorage_production_20260101_030000.archive.gz
MARIADB_RESTORE_FILE=
MONGODB_RESTORE_FILE=

# Create an extra MariaDB safety dump before restoring.
MARIADB_RESTORE_CREATE_SAFETY_BACKUP=true

# Replace target MongoDB collections before importing backup data.
MONGODB_RESTORE_DROP=true
